<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <RootNamespace>PartsPortal.Bff</RootNamespace>
    <AssemblyName>PartsPortal.Bff</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Authentication.OpenIdConnect" />
    <PackageReference Include="Stripe.net" />
    <PackageReference Include="Azure.Extensions.AspNetCore.Configuration.Secrets" />
    <PackageReference Include="Azure.Identity" />
  </ItemGroup>

  <!-- Reuses the contract DTOs + resilient HTTP wiring from the integration layer. -->
  <ItemGroup>
    <ProjectReference Include="..\..\integration\shared\Shared.csproj" />
  </ItemGroup>

</Project>
