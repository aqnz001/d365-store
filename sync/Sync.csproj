<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>PartsPortal.Sync</RootNamespace>
    <AssemblyName>PartsPortal.Sync</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\integration\shared\Shared.csproj" />
  </ItemGroup>

  <!-- Phase-1 sample BYOD catalog (Phase 2 reads real Azure SQL BYOD). -->
  <ItemGroup>
    <EmbeddedResource Include="sample-byod-catalog.json" />
  </ItemGroup>

</Project>
