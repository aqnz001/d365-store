# Mapping

Field-level mapping helpers between FinOps/BYOD/IVS shapes and Shopify / Service Bus
shapes (TDD §5). Implemented in **T4**. Keep mappings driven by the centralized
contracts in [`integration/contracts`](../../contracts) so a field-name change is a
one-place edit (Golden Rule #10).
